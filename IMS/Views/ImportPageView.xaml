<UserControl x:Class="IMS.Views.ImportPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:IMS.Views.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
             Background="{StaticResource TransactionPageBackgroundBrush}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>
    <controls:TransactionEntryShell Title="{Binding PageTitle}" VerticalAlignment="Stretch">
        <Grid Margin="0,4,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Style="{StaticResource PageSubtitle}" Text="{Binding PageDescription}" Margin="0,0,0,12" />

            <Border Grid.Row="1" Style="{StaticResource DashboardPanelCard}" Margin="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"
                               TextWrapping="Wrap" Margin="0,0,0,12" />

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Download Excel Format" Command="{Binding DownloadTemplateCommand}"
                                Style="{StaticResource PrimaryButton}" Padding="16,8" Margin="0,0,10,0" />
                        <Button Content="Browse File…" Command="{Binding BrowseFileCommand}"
                                Style="{StaticResource SecondaryButton}" Padding="16,8" Margin="0,0,10,0" />
                        <Button Content="Import Data" Command="{Binding ImportDataCommand}"
                                Style="{StaticResource PrimaryButton}" Padding="16,8" />
                    </StackPanel>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Selected file:" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox Grid.Column="1" Text="{Binding SelectedFilePath, Mode=OneWay}" IsReadOnly="True"
                                 Style="{StaticResource FormInput}" Margin="0,0,10,0" />
                        <Button Grid.Column="2" Content="{Binding TargetSectionTitle, StringFormat=Open {0}}"
                                Command="{Binding GoToSectionCommand}"
                                Style="{StaticResource SecondaryButton}" Padding="14,8"
                                Visibility="{Binding ShowGoToSection, Converter={StaticResource BoolToVisibility}}" />
                    </Grid>
                </Grid>
            </Border>

            <TextBlock Grid.Row="2" Text="{Binding SummaryText}" FontSize="13" FontWeight="SemiBold"
                       Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,8" />

            <Border Grid.Row="3" Style="{StaticResource DashboardPanelCard}" VerticalAlignment="Stretch">
                <DockPanel LastChildFill="True">
                    <TextBlock DockPanel.Dock="Top" Style="{StaticResource DashboardPanelTitle}" Text="Import log" Margin="0,0,0,8" />
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding ResultLines}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontSize="12" Margin="0,0,0,6"
                                               Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
    </controls:TransactionEntryShell>
</UserControl>
