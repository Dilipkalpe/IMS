<UserControl x:Class="IMS.Views.StockMovementReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:IMS.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1200"
             VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
             Background="{DynamicResource ContentBackgroundBrush}">
    <views:StandardReportView>
        <views:StandardReportView.FilterContent>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Date From" Style="{StaticResource SoListReportFieldLabel}" />
                    <DatePicker SelectedDate="{Binding DateFrom, Mode=TwoWay}" Height="32" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="Date To" Style="{StaticResource SoListReportFieldLabel}" />
                    <DatePicker SelectedDate="{Binding DateTo, Mode=TwoWay}" Height="32" />
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,12,0">
                    <TextBlock Text="Product Code" Style="{StaticResource SoListReportFieldLabel}" />
                    <TextBox Text="{Binding ProductCode, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" FontSize="13" />
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="0,0,12,0">
                    <TextBlock Text="Godown" Style="{StaticResource SoListReportFieldLabel}" />
                    <TextBox Text="{Binding Godown, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" FontSize="13" />
                </StackPanel>
                <StackPanel Grid.Column="4" Margin="0,0,12,0">
                    <TextBlock Text="Movement Type" Style="{StaticResource SoListReportFieldLabel}" />
                    <ComboBox ItemsSource="{Binding MovementTypeOptions}" SelectedItem="{Binding MovementType}"
                              Padding="8,6" FontSize="13" Style="{StaticResource FormCombo}" />
                </StackPanel>

                <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Style="{StaticResource SoListReportShowButton}" Command="{Binding ShowCommand}" />
                </StackPanel>
            </Grid>
        </views:StandardReportView.FilterContent>

        <views:StandardReportView.GridContent>
            <DataGrid ItemsSource="{Binding Rows}"
                      Style="{StaticResource SoListOrdersGrid}"
                      ColumnHeaderStyle="{StaticResource SoListColumnHeader}"
                      RowStyle="{StaticResource SoListRowStyle}"
                      CellStyle="{StaticResource SoListDataGridCell}"
                      AlternationCount="2"
                      MinRowHeight="42"
                      ColumnHeaderHeight="44"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Sr" Binding="{Binding SerialNo}" Width="52" />
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="100" />
                    <DataGridTextColumn Header="Entry No" Binding="{Binding EntryNo}" Width="120" />
                    <DataGridTextColumn Header="Type" Binding="{Binding MovementType}" Width="90" />
                    <DataGridTextColumn Header="From Godown" Binding="{Binding FromGodown}" Width="120" />
                    <DataGridTextColumn Header="To Godown" Binding="{Binding ToGodown}" Width="120" />
                    <DataGridTextColumn Header="Product Code" Binding="{Binding ProductCode}" Width="120" />
                    <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*" MinWidth="200" />
                    <DataGridTextColumn Header="Batch No" Binding="{Binding BatchNo}" Width="120" />
                    <DataGridTextColumn Header="In Qty" Binding="{Binding InQtyDisplay}" Width="90" />
                    <DataGridTextColumn Header="Out Qty" Binding="{Binding OutQtyDisplay}" Width="90" />
                    <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="70" />
                </DataGrid.Columns>
            </DataGrid>
        </views:StandardReportView.GridContent>
    </views:StandardReportView>
</UserControl>
