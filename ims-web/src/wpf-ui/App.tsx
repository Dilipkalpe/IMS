/* AUTO-GENERATED from WPF: App.xaml — UI only; refine for pixel parity */
import './App.scss';
import { placeholders } from '../placeholders';

export interface AppProps {
  className?: string;
}

export function App({ className }: AppProps) {
  return (
    <div className={['wpf-root', 'App', className].filter(Boolean).join(' ')} data-wpf-source="App.xaml">
    <div className="wpf-application" />
    </div>
  );
}

export default App;
