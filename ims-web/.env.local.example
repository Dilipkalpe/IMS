# Copy to .env.local for local frontend dev against the online API:
#   cp .env.local.example .env.local
#
# Base URL only — do not append /api (client paths already include /api/...).
VITE_API_BASE_URL=http://144.91.98.218:8081
