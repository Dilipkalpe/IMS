# Production build: leave empty when nginx/IIS proxies /api on the same host.
# Local dev against online API: use ims-web/.env.local (see .env.local.example).
VITE_API_BASE_URL=
VITE_BASE_PATH=/IMSWebApp/
