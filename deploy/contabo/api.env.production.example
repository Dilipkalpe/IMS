# Copy to /var/www/ims/api/.env on Contabo server
PORT=3000
HOST=0.0.0.0
MONGODB_URI=mongodb://127.0.0.1:27017/ims
IMS_AUTH_SECRET=CHANGE-ME-use-openssl-rand-hex-32
NODE_ENV=production
# Leave empty for nginx /api proxy (not IIS)
IMS_API_MOUNT_PATH=
