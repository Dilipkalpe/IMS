# Dev MongoDB — runs from the api folder (no Windows service / admin required).
storage:
  dbPath: data/mongo
  wiredTiger:
    engineConfig:
      cacheSizeGB: 0.5

systemLog:
  destination: file
  logAppend: true
  path: data/mongo.log

net:
  port: 27017
  bindIp: 127.0.0.1
